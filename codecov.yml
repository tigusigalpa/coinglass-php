ignore:
  - "tests/**/*"

coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 0%
        flags:
          - unittests
    patch:
      default:
        target: auto
        threshold: 1%
        flags:
          - unittests

comment: false
